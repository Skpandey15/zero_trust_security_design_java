plugins {
    // Lets Gradle download the Java 25 toolchain if it is not installed locally.
    id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}

rootProject.name = 'zero-trust-backend'

include 'authorization-server'
include 'resource-server'
include 'security-test-support'
